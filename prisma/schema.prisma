generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("POSTGRES_PRISMA_URL")
  directUrl = env("DIRECT_URL")
}

model Account {
  id           String   @id @default(uuid())
  name         String   @db.VarChar(255)
  email        String   @unique
  microsoftId  String   @unique
  accessToken  String?
  refreshToken String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  Session      Session?
  User         User?

  @@index([email])
  @@index([microsoftId])
}

model User {
  id        String   @id @default(uuid())
  name      String   @db.VarChar(255)
  email     String   @unique
  accountId String?  @unique
  hasVoted  Boolean  @default(false)
  jurusan   String
  fakultas  String
  himpunan  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  account   Account? @relation(fields: [accountId], references: [id], onDelete: Cascade)
}

model Session {
  id          String   @id @default(uuid())
  userId      String   @unique
  expiresAt   DateTime
  accessToken String?
  user        Account  @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VoteK3M {
  id    String @id @default(uuid())
  rank1 Int // 1st preference candidate number (1-4)
  rank2 Int // 2nd preference candidate number (1-4)
  rank3 Int // 3rd preference candidate number (1-4)
  rank4 Int // 4th preference candidate number (1-4)
}

model VoteMWAWM {
  id    String @id @default(uuid())
  rank1 Int // 1st preference candidate number (1-3)
  rank2 Int // 2nd preference candidate number (1-3)
  rank3 Int // 3rd preference candidate number (1-3)
}
